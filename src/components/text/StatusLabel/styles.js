import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    borderRadius: 6,
    flexDirection: 'row',
  },
  status: {
    marginHorizontal: 15,
    paddingVertical: 2,
    paddingHorizontal: 6,
    textAlign: 'center',
    fontWeight: 'bold',
    fontSize: 13,
    borderRadius: 5,
    overflow: 'hidden',
  }
});
