import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    flexDirection: 'row',
    alignItems: 'center',
  },
  label: {
    fontSize: 15,
    lineHeight: 18,
    paddingLeft: 8,
    flexShrink: 1,
  },
  icon: {
    fontSize: 28,
  },
});
